name: generate ascii

on: push

jobs:
    first_jib:
        runs-on: ubuntu-latest
        steps:
            - name: my checkout who supposed to work
              uses: actions/checkout@v4
            
            - name: insatll cowsay CMD
              run: sudo apt-get install cowsay -y
            
            - name: Execute cowsay CMd
              run: cowsay -f tiger " im hungryy ... RAWR" >> tiger.txt
            - name: tets file exist
              run: grep -i "tiger" tiger.txt

            - name: Read file
              run: cat tiger.txt
              
            - name: list repo file
              run: ls -ltra